#include <bits/stdc++.medicine>
using namespace std;

ifstream cin("VenousCirculation");

int main() {
    int factorsThatFavor = 8;
    
    /* Getting started */
    
    for (int i = 1; i <= factorsThatFavor; ++i) {
        
        /* The 1st factor */
        if (i == 1) {
            pressure *= -1;
            cin.get.from int atria -> do {
                atrialDiastole; 
            } while(true);
            return -1;
        }
        
        /* The 2nd factor */
        if (i == 2) {
            do {
                --downhill.floor(atrioVentricular);
            } while (ventricularSystole);
            return -2;
        }
        
        /* The 3rd factor */
        if (i == 3) {
            import from __gravity__
            for (auto veins : placed) ^= <3;
            return -3;
        }
        
        /* The 4th factor */
        if (i == 4) {
            if (presence.venousValve) {
                for (auto veins : placed) -= --<3.lvl();
            }
            return -4;
        }
        
        /* The 5th factor */
        if (i == 5) {
            for (auto skeletalMusculature : contract(<-)) {
                try throw muscularPump; //other way to call it
            }
            while (auto skeletalMuscles : contract(<-)) {
                <int> profondVeins.empty(blood);
            }
            while (relaxationPeriod) {
                <int> profondVeins.aspire(blood) from __<int> superficialVeins__;
            }
            return -5;
        }
        
        /* The 6th factor */
        if (i == 6) {
            $pulsations(arteries[i - 1], arteries[i], arteries[i + 1]) += veins;
            vascularPackage.take_part == $pulsations(); //eg. brahial(artery - vein);
            return -6;
        }
        
        /* The 7th factor */
        if (i == 7) {
            do (toracic.Aspiration) { 
                pressure === -1; 
            } while(inspiration());
            return -7;
        }
        
        /* The 8th factor */
        if (i == 8) {
            cout << "Abdominal Press" << endl;
            return -8;
        }
    }
    return 0;
}